[build-system]
requires = ["uv_build>=0.11.9,<0.12.0"]
build-backend = "uv_build"

[project]
name = "moist-bot"
version = "0.1.0"
description = "moist"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aiofiles>=24.1.0",
    "aiohttp>=3.12.14",
    "aiosqlite>=0.17.0",
    "alembic>=1.18.4",
    "anyio>=4.13.0",
    "charset-normalizer>=3.4.2",
    "colorama>=0.4.6",
    "discord-ext-menus>=1.0.0a31",
    "discord-py[speed]>=2.6.0a5228",
    "fastbloom-rs>=0.5.10",
    "jishaku",
    "mypy-extensions>=1.1.0",
    "nekos-py>=1.1.0",
    "numpy>=2.3.2",
    "orjson>=3.11.1",
    "parsedatetime>=2.6",
    "pillow>=11.3.0",
    "psutil>=7.0.0",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.14.1",
    "pyperclip>=1.9.0",
    "python-dateutil>=2.9.0.post0",
    "python-dotenv>=1.2.2",
    "pytube>=15.0.0",
    "requests>=2.32.4",
    "sqlalchemy>=2.0.49",
    "sqlmodel>=0.0.38",
    "uvloop>=0.22.1 ; os_name == 'posix'",
]

[dependency-groups]
dev = [
    "pyright>=1.1.409",
    "ruff>=0.15.14",
]

[tool.uv.sources]
discord-py = { git = "https://github.com/Rapptz/discord.py", rev = "master" }
discord-ext-menus = { git = "https://github.com/Rapptz/discord-ext-menus", rev = "master" }
jishaku = { git = "https://github.com/scarletcafe/jishaku", rev = "master" }

[project.scripts]
app = "moist_bot.__main__:main"
bot = "moist_bot.__main__:main"

[tool.pyright]
typeCheckingMode = "strict"

[tool.ruff]
line-length = 88
target-version = "py314"

[tool.ruff.lint]
preview = true
select = ["ALL"]
ignore = [
    # Handled by Ruff format
    "line-too-long",
    "blank-line-with-whitespace",
    "too-many-blank-lines",
    "missing-trailing-comma",
    "bad-quotes-inline-string",
    "extraneous-parentheses",

    # Handled by Pyright
    "private-member-access",

    # Documentation and type annotations
    "D",  # Not needed here
    "DOC",
    "any-type",
    "missing-return-type-special-method",
    "missing-return-type-undocumented-public-function",
    "missing-return-type-private-function",

    # Imports and package structure
    "implicit-namespace-package",  # Package lookup can make this annoying
    "useless-import-alias",  # Pyright requires some `as` imports
    # "typing-only-first-party-import",  # False positives with Pydantic
    # "typing-only-third-party-import",  # False positives with Pydantic
    # "typing-only-standard-library-import",  # False positives with Pydantic

    # Function and class design
    "no-self-use",  # Can easily break code
    "boolean-type-hint-positional-argument",
    "boolean-default-value-positional-argument",
    "too-many-arguments",
    "too-many-public-methods",

    # Complexity and control flow
    "complex-structure",
    "too-many-branches",
    "too-many-locals",
    "too-many-statements",
    "magic-value-comparison",  # I hate this rule
    "implicit-return-value",
    "implicit-return",  # Fine for libs like discord.py

    # Exceptions
    "suppressible-exception",
    "raise-vanilla-args",  # Over-eager rule
    "raise-within-try",  # Unrealistic rule
    "raw-string-in-exception",  # Fine for libs like discord.py

    # Async code
    "unused-async",  # Can easily break code
    "async-function-with-timeout",

    # Logging and console output
    "print",
    "logging-f-string",  # I hate this rule

    # Security
    "suspicious-subprocess-import",
    "subprocess-popen-with-shell-equals-true",
    "suspicious-non-cryptographic-random-usage",  # Shut the fuck up

    # Source and repository hygiene
    "ERA",  # Keep commented-out code
    "missing-todo-link",
    "line-contains-todo",
    "missing-copyright-notice",
    "shebang-missing-executable-file",
    "ambiguous-unicode-character-string",

    # Data models and library-specific rules
    "function-call-in-dataclass-default-argument",
    "pandas-use-of-dot-values",  # Not a pandas codebase
]
unfixable = [
    "ERA",  # Don't delete commented-out code
    "line-too-long",  # Ruff format handles line length
]

[tool.ruff.format]
quote-style = "single"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"

[tool.ruff.lint.isort]
split-on-trailing-comma = true
combine-as-imports = true

[tool.ruff.lint.pydocstyle]
convention = "numpy"

[tool.ruff.lint.flake8-annotations]
allow-star-arg-any = true

[tool.ruff.lint.flake8-pytest-style]
fixture-parentheses = false
mark-parentheses = false
parametrize-names-type = "csv"

[tool.ruff.lint.flake8-quotes]
inline-quotes = "single"
